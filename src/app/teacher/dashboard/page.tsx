import { TeacherDashboard } from "@/components/teacher/teacher-dashboard";

export const metadata = {
  title: "Teacher Dashboard | Classroom Hub",
};

export default function TeacherDashboardPage() {
    return <TeacherDashboard />;
}
