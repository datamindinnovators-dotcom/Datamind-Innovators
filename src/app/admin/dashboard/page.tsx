import { AdminDashboard } from "@/components/admin/admin-dashboard";

export const metadata = {
  title: "Admin Dashboard | Classroom Hub",
};

export default function AdminDashboardPage() {
    return <AdminDashboard />;
}
