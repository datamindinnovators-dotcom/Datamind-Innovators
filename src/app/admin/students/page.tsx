import { StudentManagement } from "@/components/admin/student-management";

export const metadata = {
    title: "Student Management | Classroom Hub",
};

export default function StudentManagementPage() {
    return <StudentManagement />;
}
